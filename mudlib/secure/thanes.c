/* Thanes.c
 * On FR a Thane is the domain leader, he has no more actions than a 
 * usual immortal but with his name has the leadership.
 */

/* You *have* to remove "bar" or it will be a security risc.
 */

string query_of(string str);

string query_of(string str)
{
 switch(str)
 {
//  case "bar":
//    return "a domain"; 
  default:
    return 0;
 }
}  
