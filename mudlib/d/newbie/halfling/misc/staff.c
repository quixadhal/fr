inherit "/std/object";


void setup()
{
set_name("staff");
set_short("staff");
set_long("The staff you hold is rather long and weighted at one end, where there seems "
"to be a place to attach a jewel.  Due to the size and weight of the staff, it is "
"not suitable as a weapon.  However, the entire staff pulses with an unearthly "
"power."
"\n\n");
add_alias("staff");
add_alias("stave");
set_main_plural("staffs");
add_plural("staffs");
add_plural("staves");
set_value(0);

set_weight(10);
}
