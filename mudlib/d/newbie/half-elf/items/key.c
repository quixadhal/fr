inherit "/std/item";

void setup() {
	set_name("Gate Key");
	set_short("Gate Key");
	add_alias("key");
        add_alias("gate key");
	set_long("This is a large key, probably to the city gate.\n");
	set_main_plural("gate keys");
	add_plural("gate keys");
}
