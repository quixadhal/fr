o Auto-update of the HOST_COMMANDS in the host list? : Failed commands will be
  removed from the list and logged.
o System responses etc. (unknown queries) (See also above).
o Improve the reliability scheme (prevent repeated tells etc.)
o Remote list updating?
o Mail.
o Times when muds are always down?
o User configurable logging / error messages.
o Sprintf instead of string addition? (#define to toggle this)
