mappingp       FR-MUD's efun help       mappingp

int mappingp(mixed arg)
     Return 1 if the argument 'arg' is a mapping, or 0 if it is not.

See also
     intp, stringp, objectp, pointerp, mkmapping, m_indices, m_values, 
     m_delete, m_sizeof
