set_exit_color
Sets the color on exits of rooms
void set_exit_color(string which);

Use this to add items to rooms that are to reappear every reset.
It does a LOT of checking, which the older clone_object()->move() code 
doesn't.
