#include <standard.h>
#include <cmd.h>
inherit CMD_BASE;

void setup()
{
position = 1;
}

static int cmd(string str, object me) {
  if (!str)
    write("Ok, trace prefix cleared.\n");
  else
    write("Ok trace prefix set to "+str+"\n");
  traceprefix(str);
  return 1;
} /* traceprefix() */

 
