inherit "/std/races/standard";

/* This is just the standard race ... but because of the problem with
   things being inherited, this is needed for now */
setup()
  {
  set_light_limits(10, 90);
}
  
