   if(this_player()->query_hb_diff(hbdiff) < LOCKOUT)
   {
      notify_fail("You don't feel calm enough to backstab.\n");
      victim->attack_by(this_player()); /* I MAY remove this later */
      return 0;
   }
